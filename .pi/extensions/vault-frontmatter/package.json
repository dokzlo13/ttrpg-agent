{
  "_comment": "pi tool registration only. The implementation, its dependencies and its lockfile live in .agents/cli/vault-frontmatter/, beside the module that require()s yaml — node resolves from there.",
  "name": "vault-frontmatter-extension",
  "private": true,
  "type": "module",
  "pi": {
    "extensions": [
      "./index.ts"
    ]
  }
}
